/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "images.ecycle.com.br",
            "images.example.com"
        ]
    }
}

module.exports = nextConfig
